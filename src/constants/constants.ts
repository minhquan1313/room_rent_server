import path from "path";

export const publicStaticServer = path.join(process.cwd(), "src", "static");

export const publicStaticUser = path.join(process.cwd(), "userDataUpload");

export const proximityThreshold = 1000; // mét
